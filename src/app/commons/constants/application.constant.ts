export enum COOKIE {
    ACCESS_TOKEN  = "_token",
    REFRESH_TOKEN = "_refresh_token",
    USER_ID = "_uid",
    USER = "user"
}

export enum REQUEST_HEADER{
    USER_ID = "x_mechat_u_id"
}

export const NG_PROGRESS_REF:string = "ngProgress"